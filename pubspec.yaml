name: vozmefit
description: Proyecto Vozmefit

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0

  # Google Sign In
  google_sign_in: ^6.2.1

  # Provider
  provider: ^6.1.2

  # UUID
  uuid: ^4.2.1

  # Iconos
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

